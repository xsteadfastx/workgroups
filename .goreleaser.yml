---
gitea_urls:
  api: https://git.xsfx.dev/api/v1/

changelog:
  filters:
    exclude:
      - "^Bump version:"
      - ^\[NO CHANGELOG\]
      - "^build:"
      - "^ci:"
      - "^repo:"

builds:
  - skip: true
